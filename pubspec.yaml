name: radio_bridge_dual
description: Радиочат через UV-82 и ESP32 (TX + RX)

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  web_socket_channel: ^3.0.3
  connectivity_plus: ^5.0.2
  network_info_plus: ^5.0.3
  #wifi_info_flutter: ^4.0.1  # ← ДОБАВЬТЕ ЭТУ СТРОЧКУ
  shared_preferences: ^2.2.2
  window_manager: ^0.5.0  # ← ВЕРНУТЬ как было

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true